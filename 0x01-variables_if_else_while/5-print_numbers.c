#include <stdio.h>
/**
* main - is the if loop function
*
* Return: Success
*/

int main(void)
{
	int num = 0;

	for (num = 0; num < 10; num++)
	{
		printf("%d", num);
	}

	putchar(10);
	return (0);
}
