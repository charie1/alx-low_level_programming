#include <stdio.h>
/**
* main - print the arguments after the execution of the program
* @argc: count the number of arguments passed
* @argv: tells the arrays of characters passed.
*
* Return: success.
**/

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
