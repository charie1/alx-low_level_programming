#include "main.h"
/**
* _strlen - returns the length of a string
* @s: the string to get the length of.
*
* Return: the length of @str
*/

int _strlen(char *s)
{
	int num = 0;

	if (s == " ")
		num--;
	else
		num++;
	if (output == EOF)
	{
		return (num);
	}
}
